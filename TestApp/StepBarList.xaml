<UserControl x:Class="TestApp.StepBarList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" HorizontalAlignment="Stretch" VerticalAlignment="Center">
    <UserControl.Resources>
        <Style x:Key="LastStepBarItemStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="-100, 0, -20, 0" />
            <Setter Property="HorizontalAlignment" Value="Right" />
        </Style>
    </UserControl.Resources>
    <Grid Name="MainGrid" Margin="20, 0" />
</UserControl>
